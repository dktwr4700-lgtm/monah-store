# Firebase Security Audit Notes

## مصدر القواعد

شارك مالك الموقع قواعد Firestore الحالية من Firebase Console في هذه المهمة بتاريخ 26 أغسطس 2026. لم تُعدّل القواعد أثناء الفحص.

## نتائج أولية من القواعد الحالية

| المورد | الحالة الظاهرة | مستوى المخاطرة | السبب |
|---|---|---:|---|
| `products` | أي مستخدم مسجل يستطيع إنشاء أو تحديث أي مستند | مرتفع | لا توجد مطابقة بين معرف المستخدم و`ownerId` عند الإنشاء أو التعديل. |
| `products/{product}/codes` | كل مستخدم مسجل يستطيع إنشاء أكواد | مرتفع | لا توجد مطابقة بين المستخدم ومالك المنتج عند الإنشاء. |
| `orders` | أي زائر يستطيع إنشاء طلب | مرتفع | لا يشترط تسجيل الدخول ولا يطابق صاحب الطلب مع المستخدم. |
| `shared` | قراءة وكتابة عامة | مرتفع | مفتوح للإنترنت دون تحقق، ولا يظهر استعماله في الكود الحالي. |
| لوحة الإدارة | البريد محمي في الواجهة فقط | مرتفع | حماية الواجهة لا تكفي من دون شرط خادمي أو Rules يعتمد على Custom Claim. |
| `stores` | المالك يكتب مستنده فقط | متوسط | يحتاج ضبط حقول يسمح بتعديلها؛ القراءة العامة مقصودة للمتجر العام. |
| `sellers` | المالك أو بريد الإدارة يقرأ/يكتب | متوسط | شرط البريد قد يعمل في القواعد، لكنه يحتاج حصر الحقول ومنع رفع الباقة أو الصلاحيات من العميل. |
| `unlocks` | المستخدم ينشئ تصريح تنزيل لنفسه | مرتفع | يسمح للمستخدم بمنح نفسه تصريح تنزيل ما لم يمنع المسار الخادمي الاعتماد على هذا المستند. |

## ما لم يُفحص بعد

قواعد Firebase Storage أصبحت متاحة للمراجعة. ما زالت إعدادات Authentication الفعلية غير متاحة في التقرير. فحصها يحتاج عرضًا قرائيًا منفصلًا من Firebase Console، ولا يتطلب أي تعديل أو نشر.

## اختبار قراءة فقط

أُجري في الفحص طلب `GET` واحد لكل مجموعة مستخدمًا اسم مستند وهمي غير موجود، ولم يُنشأ أو يُعدّل أو يُحذف أي مستند. أعاد Firestore `404 Not Found` لمجموعتي `products` و`shared`، وهو متسق مع القراءة العامة المسموحة. وأعاد `403 Permission Denied` لمجموعات `sellers` و`orders` و`unlocks` بلا جلسة مستخدم، وهو متسق مع اشتراط الجلسة. لم تُستخدم استعلامات قائمة البيانات ولم تُقرأ بيانات عملاء أو بائعين حقيقية.

## ملاحظات قواعد Storage

قواعد مسار `secure` تربط القراءة بمالك المنتج أو بتصريح `unlock` ساري، وتربط الكتابة بمالك المنتج؛ وهذا أساس جيد، لكنه يعتمد بالكامل على منع العميل من إنشاء مستند `unlock` بنفسه. مسارات الصور العامة تقصر اسم الملف على بادئة المستخدم، لكنها لا تتحقق من أن المحتوى صورة ولا تطابق حد واجهة الموقع البالغ 5 ميغابايت. كما لا توجد قواعد صريحة تفصل الإنشاء والتحديث والحذف، ولذلك ستُقترح قواعد أوضح لهذه العمليات.

## ملاحظات Authentication

تظهر شاشة إعداد Authentication أن Email/Password هو مزود الدخول الوحيد المفعّل. Google Sign-In غير مفعّل، ولا يظهر Anonymous Sign-In ضمن المزودات الظاهرة. لا يلزم تفعيل Google Sign-In لهذه المنصة الآن، ولا يدل التنبيه على وجوب تغييره ضمن فحص الأمان الحالي.
