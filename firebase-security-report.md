# تقرير فحص أمان Firebase — مُونَة

## نطاق الفحص

تمت مراجعة قواعد **Cloud Firestore** التي شاركها المالك، ثم مقارنتها بتدفقات الموقع الفعلية: الحسابات، المنتجات، الأكواد، المتاجر، الطلبات، التنزيل، ولوحة الإدارة. أُجري اختبار قراءة واحد فقط على أسماء مستندات وهمية وغير موجودة؛ لم يُنشأ أو يُعدّل أو يُحذف أي مستند، ولم يتم فحص بيانات تجار أو عملاء حقيقية.

> **لم يتغير الدفع، ولم تتغير قواعد Firebase، ولم تتغير بيانات الإنتاج أو إعدادات المصادقة أثناء هذا الفحص.**

## النتيجة المختصرة

الموقع يعمل، لكن **قواعد Firestore الحالية ليست كافية لفتح البيع الحقيقي بأمان**. توجد صلاحيات تسمح لمستخدم مسجّل بإنشاء أو تعديل موارد لا تخصه، وبوصول واسع إلى أكواد المنتجات غير المستخدمة. قبل فتح الدفع أو تسليم الملفات تلقائيًا، يلزم تقوية القواعد وفصل بيانات المتجر العامة عن بيانات السحب الخاصة.

| الأولوية | المورد | الخطر الحالي | الأثر المحتمل | الحل المقترح |
|---|---|---|---|---|
| مرتفع | `products` | أي مستخدم مسجّل يستطيع إنشاء أو تحديث منتج دون مطابقة `ownerId` | تعديل سعر أو بيانات أو ملف منتج تاجر آخر | قصر الإنشاء والتعديل والحذف على صاحب `ownerId`، ومنع تغيير حقل المالك أو التعليق أو حقول النظام. |
| مرتفع | `products/{id}/codes` | الأكواد غير المستخدمة قابلة للقراءة، وأي مستخدم مسجّل يستطيع إنشاء أكواد | كشف أكواد تراخيص وبيعها أو إضافتها لمنتجات الآخرين | قصر القراءة والإنشاء على مالك المنتج، ومنع تحديث حالة الاستخدام من المتصفح؛ خادم موثوق فقط يخصص الكود للمشتري. |
| مرتفع | `orders` | أي زائر يستطيع إنشاء طلب، والمالك يستطيع تعديل معظم حقوله | طلبات مزيفة أو تغيير السعر أو البريد أو حالة الطلب | منع إنشاء الطلبات من العميل. بعد ربط ثواني، ينشئ الخادم الطلب بعد تحقق الدفع. ويُسمح للبائع فقط بتغيير حالة التسليم المحددة مؤقتًا إن بقيت العملية اليدوية. |
| مرتفع | `unlocks` | المستخدم يستطيع إنشاء تصريح تنزيل لنفسه | منح النفس إمكانية تنزيل ملف مدفوع | منع كل كتابة من العميل؛ الخادم فقط ينشئ التصريح بعد دفع موثّق. |
| متوسط | `coupons` | لا توجد قاعدة مطابقة للمجموعة رغم أن لوحة البائع تستخدمها | ميزة الكوبونات قد تتعطل افتراضيًا أو تحتاج قاعدة غير معروضة | إضافة قاعدة مقيّدة للمالك وباقة مؤهلة، ثم جعل التطبيق الحقيقي للكوبون خادميًا مع الدفع. |
| مرتفع | `shared` | قراءة وكتابة عامة | إدخال أو استبدال محتوى من أي شخص عبر الإنترنت | إغلاق المجموعة بالكامل ما لم يكن لها استخدام معروف ومحدد. لا يظهر استعمال لها في الكود الحالي. |
| مرتفع | لوحة الإدارة | التحقق من بريد الإدارة موجود في الواجهة فقط | واجهة الإدارة ليست حاجز حماية كافيًا بمفردها | استخدام **Custom Claim** إداري من خادم موثوق، ثم اشتراطه في قواعد Firestore. |
| متوسط | `stores` | قراءة عامة تشمل حقولًا قد تحتوي معلومات سحب يدوي | ظهور بيانات السحب أو التحويل إذا كانت مخزنة داخل المستند العام | نقل بيانات السحب إلى مستند خاص لا يقرأه إلا مالكه والإدارة؛ يبقى الاسم والشعار والوصف فقط في المتجر العام. |
| متوسط | `sellers` | المالك يستطيع الكتابة في مستنده دون قصر الحقول | قد يغير العميل الباقة أو حالة الإيقاف إن عدّل الطلب مباشرة | السماح بتعديل الاسم فقط من العميل، وجعل الباقة والحالة وحقول الإدارة خادمية فقط. |
| مرتفع | `secure` في Storage | حماية التنزيل تعتمد على مستند `unlock` الذي يمكن للعميل إنشاؤه حاليًا | يمكن تجاوز حماية الملف عبر ثغرة `unlocks` في Firestore | إغلاق كتابة `unlocks` من العميل، وجعل الخادم فقط ينشئه بعد دفع موثق. |
| متوسط | صور المنتجات والشعارات والأغلفة | القواعد تتحقق من الاسم والحجم فقط، لا من نوع الملف، ولا تفصل الإنشاء والتحديث والحذف | يمكن رفع ملف غير صورة ضمن المسارات العامة، ولا تتطابق القواعد مع حد الواجهة | قصرها على صورة وبحد 5 ميغابايت، والسماح لصاحب البادئة فقط بالإنشاء والتحديث والحذف. |
| منخفض | Authentication | Email/Password هو المزود الوحيد الظاهر والمفعّل؛ Google غير مفعّل ولا يظهر Anonymous Sign-In | لا توجد طريقة دخول إضافية ظاهرة تحتاج إغلاقًا الآن | إبقاء Google وAnonymous غير مفعّلين ما لم تضاف ميزة تحتاجهما. |

## ما تأكد من الاختبار القرائي

أعاد Firestore `404 Not Found` عند قراءة اسم مستند وهمي ضمن `products` و`shared` بلا جلسة، وهو متسق مع القراءة العامة في القواعد الحالية. وأعاد `403 Permission Denied` للمستندات الوهمية ضمن `sellers` و`orders` و`unlocks` بلا جلسة، وهو متسق مع اشتراط دخول المستخدم. لم تُنفذ استعلامات قائمة، ولم تُقرأ بيانات حقيقية.

## خطة التقوية الآمنة

لا ينبغي نشر قواعد صارمة دفعة واحدة، لأن صفحة المتجر العام الحالية تستعلم عن المنتجات بطريقة تحتاج تعديلًا صغيرًا في الواجهة حتى لا تتوقف. التسلسل الآمن هو:

| المرحلة | ما يتغير | لماذا |
|---|---|---|
| 1 | تعديل استعلام المنتجات العامة ليطلب المنشور وغير الموقوف فقط | حتى تستمر المتاجر العامة في العمل بعد إغلاق المنتجات المخفية. |
| 2 | فصل معلومات السحب اليدوي عن مستند `stores` العام | حتى لا تظهر أي معلومات خاصة للزائر. |
| 3 | إضافة Custom Claim للإدارة عبر الخادم | حتى تصبح الإدارة محمية من الخلف لا من الواجهة فقط. |
| 4 | نشر قواعد Firestore الجديدة واختبار بائع وزائر غير مسجل وحالة فشل | للتحقق من عدم السماح بتعديل أو قراءة غير مصرح بها. |
| 5 | مراجعة قواعد Storage وتقييد رفع الصور والملفات بالمسار والحجم والنوع | لحماية ملفات المنتجات، مع استمرار رابط التحميل القصير الذي ينشئه الخادم. |
| 6 | عند جاهزية ثواني، نقل إنشاء الطلبات وتراخيص التنزيل إلى الخادم بعد تحقق الدفع | حتى لا يستطيع أي زائر إنشاء طلب أو تصريح تنزيل من المتصفح. |

تعتمد الخطة على قواعد الحقول `hasOnly()` و`diff().affectedKeys()` لحصر ما يستطيع العميل إنشاؤه أو تغييره، وهي آلية موصى بها رسميًا لحماية حقول Firestore [1]. كما أن التحكم في أدوار الإدارة عبر Custom Claims يجب أن يدار من خادم موثوق، لأن SDK الإداري يملك صلاحية مرتفعة ويتجاوز قواعد Firestore [2]. وقواعد Storage تستطيع تقييد الرفع حسب المسار ونوع الملف وحجمه [3].

## ما أحتاجه لإكمال الفحص قبل أي تغيير

تمت مراجعة Storage Rules وطرق Authentication الظاهرة. الفحص القرائي مكتمل؛ الخطوة التالية هي مراجعة قواعد التقوية المقترحة قبل تطبيق أي تغيير.

بعدها أعرض قواعد Firestore وStorage المقترحة بصيغة جاهزة للمراجعة. لن تُطبّق إلا بعد موافقتك الصريحة المنفصلة على تغيير Firebase.

## المراجع

[1]: https://firebase.google.com/docs/firestore/security/rules-fields "Firebase: التحكم في حقول Firestore من Security Rules"
[2]: https://firebase.blog/posts/2019/03/firebase-security-rules-admin-sdk-tips/ "Firebase: Security Rules وAdmin SDK والأدوار"
[3]: https://firebase.google.com/docs/storage/security "Firebase: قواعد أمن Cloud Storage"
