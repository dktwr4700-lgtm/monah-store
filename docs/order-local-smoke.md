# فحص محلي لتدفق الطلبات

- التاريخ: 2026-09-03.
- الصفحة الرئيسية المحلية فتحت بعد إضافة ملفات الطلبات.
- صفحة المنتج الموجودة `bcD0BPTr52COsSPPBOMs` فتحت محليًا كمنتج مجاني، واستمر زر التنزيل المجاني ظاهرًا.
- صفحة المنتج المدفوع الموجودة `cAlTFYDTcIxaAafk7v6q` فتحت محليًا بسعر 50.00 ر.ع، وظهر فيها زر «اطلب المنتج داخل مُونَة» بدل رابط واتساب.
- فتح زر الطلب محليًا أظهر خطوة البريد ثم زر «متابعة للتحويل»، مع تنبيه صريح بأن نسخة التجربة تربط المتابعة بهذا الجهاز والمتصفح.
- لم يُضغط زر الطلب للمنتج المدفوع، ولم يُنشأ طلب أو إثبات أو تنزيل خلال الفحص.
- لم يُنشأ طلب، ولم يُرفع إثبات، ولم تُعدّل أي بيانات فعلية خلال هذا الفحص.
- يلزم اختبار منتج مدفوع مخصص بعد تجهيز بيانات تحويل اختبارية وموافقة واضحة على إنشاء بيانات الاختبار.
