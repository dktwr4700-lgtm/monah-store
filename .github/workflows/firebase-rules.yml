name: Firebase rules

# يتحقق من firestore.rules و storage.rules بكل PR يغيّرها، وينشرها لـ Firebase بعد الدمج في main.
on:
  pull_request:
    paths:
      - firestore.rules
      - storage.rules
      - scripts/deploy-firebase-rules.mjs
      - .github/workflows/firebase-rules.yml
  push:
    branches: [main]
    paths:
      - firestore.rules
      - storage.rules
      - scripts/deploy-firebase-rules.mjs
      - .github/workflows/firebase-rules.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: firebase-rules-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  rules:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Check rules
        if: github.event_name == 'pull_request'
        run: node scripts/deploy-firebase-rules.mjs --check
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      - name: Deploy rules
        if: github.event_name != 'pull_request'
        run: node scripts/deploy-firebase-rules.mjs
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
