# نتائج الفحص الأولي لموقع مُونَة

## الحالة الفنية

نجح بناء نسخة الإنتاج محليًا باستخدام Vite. أظهر البناء تحذيرًا بأن حجم ملف JavaScript النهائي كبير نسبيًا، ولا توجد حاليًا اختبارات آلية أو ملف قفل للتبعيات في النسخة الأساسية؛ تم توليد ملف القفل محليًا أثناء التجهيز ليصبح التثبيت قابلاً للتكرار.

## الواجهة التي فُحصت

ظهرت الصفحة الرئيسية وشاشة إنشاء الحساب بصورة سليمة بصريًا في معاينة سطح المكتب. روابط تسجيل الدخول وإنشاء المتجر تصل إلى شاشة التسجيل، لكن الفحص البرمجي كشف أن مسار صفحة المنتج العام لا يحمّل مكوّن صفحة منتج حقيقيًا، كما توجد وعود ظاهرة تتعلق بالدفع والتسليم لا ينبغي تعديلها ضمن هذا النطاق المستثنى من الدفع.

## مشكلات مؤكدة قبل الإصلاح

- رابط المنتج العام يحمّل مكوّن مساعد النمو بدل صفحة المنتج، ولذلك لا يستطيع العميل عرض تفاصيل المنتج أو إكمال تدفق شراء صحيح.
- المتجر العام يستبعد المنتجات المخفية فقط ولا يستبعد المنتجات الموقوفة إداريًا.
- إعداد تصميم المتجر يقبل رابطًا فارغًا ويقترح رابط متجر بالمعرّف الداخلي، ما يضعف تجربة مشاركة رابط المتجر.
- لوحة البائع تحتوي على واجهة تشخيص تعرض تفاصيل الخطأ الداخلية للمستخدم عند فشل الواجهة.
- لا يوجد ملف قفل للتبعيات في النسخة الأساسية، لذلك يفشل الأمر `npm ci` الذي يلزم للتثبيت القابل للتكرار.

## نتائج اختبار المسارات

عند فتح رابط منتج تجريبي في المعاينة، ظهرت شاشة بيضاء مع زر مساعد عائم فقط؛ هذا يؤكد أن المسار معطّل من منظور العميل وليس مجرد مشكلة في بيانات المنتج. أما شاشة تسجيل الدخول فتظهر حقول البريد وكلمة المرور ورابط الاستعادة بوضوح، ولم يُجرَ أي تسجيل دخول أو إنشاء حساب تجريبي حتى لا تتأثر بيانات Firebase.

## نتائج الاعتمادية والأمن البرمجي

فحص `npm audit` أظهر 18 ملاحظة معلنة في تبعيات الإنتاج، منها ملاحظة عالية الخطورة ضمن سلسلة التبعيات المستخدمة من Firebase. يلزم تحديث تبعيات Firebase وFirebase Admin بحذر ثم إعادة فحصها. كما أن واجهة المساعد الذكي تعتمد على بيانات خطة تصل من المتصفح ولا تتحقق من هوية البائع على الخادم؛ لذلك لا ينبغي اعتبارها بوابة صلاحيات كافية قبل إصلاحها.

## التحقق بعد الإصلاح الأولي

بعد استبدال مسار المنتج المعطّل بصفحة منتج فعلية، أصبح الرابط غير الصحيح يعرض رسالة عربية واضحة مع خيار العودة إلى الصفحة الرئيسية بدل شاشة بيضاء. كما نجح البناء بعد فصل الصفحات الثقيلة إلى ملفات تحميل مستقلة، وانخفض حجم حزمة الصفحة الرئيسية من نحو 959 كيلوبايت إلى نحو 351 كيلوبايت قبل الضغط. بعد تحديث Firebase وFirebase Admin لم يعد فحص التبعيات يعرض أي تنبيه عالي الخطورة؛ بقيت ست ملاحظات متوسطة ضمن سلسلة تبعيات Firebase Admin الخارجية رغم استخدام أحدث إصدار مستقر.

تم أيضًا فحص رابط متجر غير موجود؛ بعد اكتمال التحميل يظهر تنبيه واضح للزائر مع زر للعودة إلى مُونَة، بدل عرض متجر عام فارغ بلا تفسير.

تأكدت معاينة سطح المكتب كذلك من استمرار عمل الصفحة الرئيسية وانتقال «أنشئ متجرك» إلى شاشة التسجيل بعد فصل الصفحات إلى تحميل مؤجل. لم يُنشأ أي حساب أو منتج تجريبي ضمن Firebase أثناء الفحص.

نجحت أيضًا مراجعة الوصول الخارجي للنسخة المحلية عبر رابط معاينة مؤقت بعد تقييد إعداد Vite على نطاق المعاينة المحدد؛ الصفحة الرئيسية متاحة من الرابط ولا يجري هذا الإعداد أي نشر أو تعديل للإنتاج.
